{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "tiny-thinkers",
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import http.server as s\nclass H(s.SimpleHTTPRequestHandler):\n    def end_headers(self):\n        self.send_header('Cache-Control', 'no-store')\n        super().end_headers()\ns.ThreadingHTTPServer(('', 8765), H).serve_forever()"
      ],
      "port": 8765
    }
  ]
}
